spring:
  application:
    name: springRestMvc
    
  flyway:
    enabled: false  

controllers:
  customer-controller:
    request-path: "/api/v1/customer"
  beer-controller:
    request-path: "/api/v1/beer"
